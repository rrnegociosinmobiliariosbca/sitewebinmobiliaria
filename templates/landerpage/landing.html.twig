{% extends 'base.html.twig' %}

{% block title %}RR NEGOCIOS INMOBILIARIOS
{% endblock %}

{% block body %}
	<style>
		html,
		body {
			height: 100%;
			margin: 0;
			padding: 0;
		}

		body {
			background-image: url('/img/fondo.jpeg');
			background-attachment: fixed;
			background-size: cover;
			background-repeat: no-repeat;
			background-position: center;
		}

		.navbar {
			position: fixed;
			top: 0;
			width: 100%;
			z-index: 1030;
			background-color: rgba(0, 176, 240, 1);
		}

		.content-wrapper {
			margin-top: 150px; /* Altura del navbar */
		}

		.gallery-section {
			padding: 60px 20px;
			background-color: rgba(27, 15, 15, 0.3); /* fondo semi-transparente */
			border-radius: 10px;
			margin-bottom: 30px;
		}

		.gallery-section-fix {
			padding: 60px 20px;
			background-color: rgba(255, 255, 255, 1); /* fondo semi-transparente */

		}

		.section-banner {
			padding: 10px;
			background-color: rgba(255, 255, 255, 1); /* fondo semi-transparente */
			margin-bottom: 0;
		}

		footer {
			background-color: #222;
			color: #fff;
			text-align: center;
			padding: 20px 10px;
			position: relative;
			bottom: 0;
			width: 100%;
		}
	</style>

	<!-- Navbar -->
	<nav class="navbar navbar-expand-lg" data-bs-theme="dark">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{ path("app_") }}">
				<img src="/img/logo.png" alt="Logo" width="48">
				<span class="ms-2">RR NEGOCIOS INMOBILIARIOS</span>
			</a>
		</div>
	</nav>

	<!-- Contenido principal -->
	<div class="content-wrapper">
		<section class="gallery-section">
			<div class="container text-center">

				<img src="/img/logo.png" alt="Imagen única" class="img-fluid w-75 mb-4">

				<h2 class="mb-4 mt-4 text-white text-uppercase">
					<a href="{{ path("app_buscar") }}" class="text-white text-decoration-none fw-bold">
						Click para continuar
					</a>
				</h2>

			</div>
		</section>
	</div>

{% endblock %}
